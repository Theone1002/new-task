# Claude Code 桌面端与移动端串联指南

## 现状：数据是分开的

目前 Claude Code 的**电脑端**和**手机端**会话数据是**独立的**：

| 平台 | 运行方式 | 数据存储 | 能力 |
|------|---------|---------|------|
| 电脑端 | CLI / 桌面应用 / IDE 插件 | 本地 | 直接操作本地文件系统 |
| 手机端 | claude.ai/code 网页 / GitHub | 云端 | 只能操作云端沙箱环境 |

**核心问题**：两端的会话历史、项目上下文、文件状态都不互通。你在手机上开的会话，电脑上看不到；反之亦然。

## 现有的串联方案

虽然不能直接共享会话状态，但有以下方式可以间接实现协作：

### 方案 1：通过 Git 仓库同步（推荐）

Git 是目前最实用的桥梁。两端操作同一个 GitHub 仓库：

```
手机端 ──push──► GitHub 仓库 ◄──pull── 电脑端
```

- 手机端：在 claude.ai/code 中修改代码，commit + push 到 GitHub
- 电脑端：`git pull` 拉取手机端的修改，继续开发
- 反过来也一样

**优点**：简单可靠，代码变更有完整历史记录
**缺点**：会话上下文（对话历史）不同步，只有代码同步

### 方案 2：Remote Triggers（远程触发器）

从手机端远程触发电脑端的 Claude Code 执行本地任务：

```
┌─────────────────┐                    ┌─────────────────┐
│  手机端           │   Remote Trigger   │  电脑端（本地）   │
│  claude.ai/code  │ ─────────────────► │  Claude Code CLI │
│                  │ ◄───────────────── │  本地文件系统     │
│                  │   结果返回          │                  │
└─────────────────┘                    └─────────────────┘
```

适用场景：
- 出门在外，用手机触发电脑端跑测试
- 手机端审查代码，触发电脑端部署
- 手机端查看本地 Git 状态

### 方案 3：CLAUDE.md 项目文件共享上下文

在项目根目录创建 `CLAUDE.md` 文件，写入项目的关键信息：

```markdown
# 项目说明
- 这是一个 Python 图片处理工具
- 主要功能：替换海报中的二维码
- 测试命令：pytest
- 构建命令：python setup.py build
```

这样无论在手机端还是电脑端打开项目，Claude Code 都会读取这个文件，保持对项目的基本理解一致。

**优点**：项目知识在两端共享
**缺点**：不是实时会话同步

## 推荐工作流

```
1. 电脑端：主要开发，写代码、调试、测试
      │
      ├── commit + push 到 GitHub
      │
2. 手机端：轻量操作，代码审查、小修改、触发任务
      │
      ├── commit + push 到 GitHub
      │
3. 电脑端：git pull，继续开发
```

## 期望 vs 现实

| 功能 | 现状 | 说明 |
|------|------|------|
| 共享会话历史 | 不支持 | 两端会话完全独立 |
| 共享文件状态 | 通过 Git | 需要手动 push/pull |
| 远程触发操作 | 支持 | 通过 Remote Triggers |
| 共享项目配置 | 支持 | 通过 CLAUDE.md 文件 |
| 实时协作 | 不支持 | 不能同时编辑同一文件 |

## 总结

目前桌面端和移动端**确实是分开的**，不能无缝串联。最实用的方式是：

1. **用 Git 同步代码**（两端操作同一仓库）
2. **用 CLAUDE.md 共享项目上下文**（让两端的 Claude 都了解项目）
3. **用 Remote Triggers 远程触发任务**（手机端控制电脑端执行）

这些方案加在一起，可以实现比较流畅的跨设备开发体验，但还不是真正的"无缝连接"。
